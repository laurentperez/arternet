var assert = require('assert');
describe('Game', function() {
  describe('#compute()', function () {
    it('should compute', function () {
      var Game = require('../src/client/app');
      assert.equal(1,1);
    });
  });
});